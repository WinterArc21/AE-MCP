# AE MCP - environment variables
# Copy to .env and adjust, or set in your shell / MCP client config.

# Command timeout in milliseconds for non-render operations.
# Default is 120000 (2 minutes).
# AE_MCP_TIMEOUT=120000
